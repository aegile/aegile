import DndListContext from "./_components/DndListContext";

export default async function ProjectsTaskPage() {
  return (
    <main className="grid flex-grow items-start p-4 sm:px-6 sm:py-0">
      <DndListContext />
    </main>
  );
}
